{
  "name": "aws-lambda-slm-processor",
  "version": "0.1.0",
  "description": "Utilizes dynamodb while executing as a lambda asset on AWS",
  "main": "dist/index.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "generate": "tsc",
    "build": "npm run prebuild && npm run generate",
    "start": "node .",
    "package": "echo \"Error: Not yet implemented. This will eventually generate an 'aws-lambda-slm-processor.zip` in the project directory.\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluedoge/aws-lambda-slm-processor.git"
  },
  "keywords": [
    "lambda",
    "aws",
    "dynamodb"
  ],
  "author": "Elizabeth Clements",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bluedoge/aws-lambda-slm-processor/issues"
  },
  "homepage": "https://github.com/bluedoge/aws-lambda-slm-processor#readme",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.100.0",
    "@aws-sdk/types": "^3.78.0",
    "@types/aws-lambda": "^8.10.97",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.36",
    "tslint": "^6.1.3",
    "typescript": "^4.7.2"
  }
}
